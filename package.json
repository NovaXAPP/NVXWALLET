{
  "name": "nvx-wallet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build --config vite.config.cjs",
    "clean": "rimraf dist && rimraf node_modules/.vite"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0"
  }
}
